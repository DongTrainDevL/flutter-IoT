import 'package:flutter/material.dart';

class SimulatePushDATAByHardware extends StatelessWidget {
  const SimulatePushDATAByHardware({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'SimulatePush',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
