import 'package:flutter/material.dart';

class DashboardManagePage extends StatelessWidget {
  const DashboardManagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'DashboardManagePage',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
