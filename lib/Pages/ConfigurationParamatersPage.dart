import 'package:flutter/material.dart';

class ConfigurationParameterPage extends StatelessWidget {
  const ConfigurationParameterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ConfigurationParamaterPage',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
